# AGENTS.md (repo-wide)

이 저장소에서 작업할 때는 이 `AGENTS.md`를 최우선으로 따른다. 프로젝트 컨텍스트/템플릿은 `CODEX.md`를 참조한다.

## 0) 최우선 준수 문서
- `CODEX.md` (필수): 분량/스타일, 코드 제시 원칙, "실제 실행 결과만" 사용, 크로스 플랫폼 호환, 참고문헌 검증, 폴더 구조
- `docs/sample.md` (필수): 본문 서술·표·핵심 코드(3~5줄) 제시 스타일 표준
- `contents.md` (필수): 각 장의 절 구성/집필 방향/강화 항목(요구사항)
- `lecture/README.md` (학부 강의교재 작성 시 필수): 실무교재를 근거로 쉬운 직관·비유 중심 강의교재를 만드는 파생 규칙

## 0.1) 레거시 지침(참고)
아래 레거시 지침은 참고 자료로 보관한다. 단, 작성 형식/편집 스타일은 본 `AGENTS.md`의 “강의 교재 모드” 규칙을 우선 적용한다.
- 기획: `.codex/skills/book-planner/references/legacy-planner.md`
- 리서치: `.codex/skills/book-researcher/references/legacy-researcher.md`
- 집필: `.codex/skills/book-writer/references/legacy-writer.md`
- 실습코드: `.codex/skills/book-coder/references/legacy-coder.md`
- 검토: `.codex/skills/book-reviewer/references/legacy-reviewer.md`
- 그래픽: `.codex/skills/book-graphic/references/legacy-graphic.md`

## 0.2) 사용자 지정 예외(우선 적용)
- 사용자 지시로 `docs/ch1.md`, `docs/ch2.md`, `docs/ch3.md`는 복구하지 않는다. 최종 원고 파일명은 사용자 지시를 따른다.

## 1) 절대 금지 (하드 룰)
- 더미/가상 데이터로 결과를 "만들지" 않는다.
- "예시 출력입니다" 같은 가짜 실행 결과를 본문에 쓰지 않는다.
- 허구의 참고문헌/URL/DOI를 쓰지 않는다. 검증 가능한 실제 문헌만 사용한다.
- 플랫폼 종속 경로/명령어를 본문/코드에 하드코딩하지 않는다.

## 2) 강의 교재 모드(필수)
이 저장소의 원고는 “강의 교재용” 편집을 기본값으로 한다. 학습자가 빠르게 훑고 이해할 수 있도록, 서술형 긴 문단보다 구조화(소제목/불릿/표/체크리스트)를 우선한다.

### 2.1 고정 템플릿(각 절 공통)
각 절(예: 1.1, 1.2 …)은 가능하면 다음 순서를 유지한다.
1. **학습 목표(요약)**: 2–4줄(불릿 허용)
2. **핵심 개념**: 5–9개 불릿(정의/원리/주의점)
3. **공공 사례**: 1개(짧은 서술 + 핵심 포인트 불릿)
4. **체크 질문**: 2–3개(수업 질문/퀴즈)
5. **핵심 정리**: 3–5개 불릿

### 2.2 문단/줄바꿈 규칙
- 본문은 **짧은 단락(2–4문장)** 을 기본으로 허용/권장한다.
- 긴 논지는 문단을 늘리기보다 **소제목을 더 쪼개고 불릿/표**로 구조화한다.
- 문단 내부의 문장 단위 줄바꿈은 하지 않는다(문단은 빈 줄 1개로만 구분).

### 2.3 서두(필수)
- 장의 시작은 **도입 문단 1개**로 시작하며, `contents.md`의 학습 목표를 **불릿 복사 없이 3–6문장으로 풀어서 서술**한다.
- 도입 직후에는 **요약형 `## 학습 목표` 섹션**을 둘 수 있다(강의용 요약 박스 역할). 단, `contents.md` 불릿을 그대로 복사해 붙이지 말고 표현을 다듬는다.

### 2.4 구체성·최신성 규칙 (2026-07-11 사용자 지시 — 전 장 재작성·보완의 기준)

기존 원고가 "내용을 요약한 문서"처럼 읽히는 문제를 고치기 위한 규칙이다. 1장부터 순서대로 적용하되, **아래 모든 항목(구체 서술·최신 이론·현장 방법·민간→공공 번역·codex 검토)은 1장에 한정되지 않고 17장 전체에 해당한다.**

1. **요약체 금지, 구체 서술 의무**: 추상 개념을 압축한 용어 나열이나 한 줄 정의로 끝내지 않는다. 새 개념은 ① 정의 → ② 왜 필요한가(없으면 무엇이 깨지는가) → ③ 구체 시나리오(누가·언제·어떤 데이터에서 어떻게 깨지는가) → ④ 현장에서 실제로 쓰는 방법(도구·절차·판단 기준) 순으로 풀어쓴다. 불릿 한 줄이 문장 하나의 개념 압축이라면, 그 불릿은 본문 단락으로 풀어야 할 후보다.
2. **최신 이론·현장 반영**: 각 장에 (a) 최근 이론·프레임워크(예: 데이터 계약, MLOps 성숙도 모델, LLMOps)와 (b) 현장의 의견·방법(실무자 인터뷰 연구, 기업 기술 블로그, 공개 포스트모템, 표준·가이드 문서)을 수록한다. 모든 인용은 URL 실검증을 통과한 실재 출처만 쓰고(§1 하드룰), 적용 자격(직접 선례/방법론 차용/배경 원칙)을 구분해 서술한다.
3. **분량 하한(상한 없음)**: 장당 **최소 400줄**은 하한일 뿐이다(1~3장 포함 전 장 적용 — 2026-07-07 결정의 확정판). **담을 내용이 있으면 분량에 제한받지 않고 담는다** — 장 수는 17장으로 한정되어 있으므로 각 장의 내용을 풍부하게 채운다(2026-07-11 사용자 지시). 단 억지 분량(padding) 금지는 유지한다. 길이는 구체 시나리오·워크드 예제·현장 방법 서술·실측 관찰에서 나와야 한다.
4. **실측 증거 불변**: 재작성·보완 시 기존 실측 수치·증거 JSON·장 간 교차참조(피처 지문, app sha256 등)는 변경하지 않는다. 새 수치가 필요하면 실제 실행으로만 만든다. 실측 수치가 있는 장은 수정 후 수치 대조를 재수행한다.
5. **설계 선행**: 장 재작성·보완 착수 전 `schema/chap{N}.md`를 갱신해 절별 확장 설계(무엇을 구체화하는지, 어떤 이론·현장 방법을 추가하는지, 목표 분량)를 먼저 확정한다. 설계 없이 본문 수정을 시작하지 않는다.
6. **codex 검토 의무**: 장 작성·보완 완료 후 반드시 codex 검토(파일 범위 `codex exec` 또는 `codex review`)를 받고, 타당한 지적은 점수·심각도와 무관하게 반영한다. 반영 후 실측 수치가 걸린 장은 수치 대조를 재수행한다. codex가 클린 판정을 내리기 전에는 해당 장을 "완료"로 보고하지 않는다.
7. **민간 비즈니스 로직의 공공 번역**: 공공에서도 실시간 처리는 매우 중요하지만 비즈니스 영역만큼 다뤄지지 않는다. 각 장은 민간 현장의 실시간·운영 방법(광고 실시간 집계, 추천 피처 공급, A/B 테스트, 사업 지표 대시보드 등)을 소개하고 이를 공공 맥락으로 창의적으로 번역해 수록한다 — 공공을 공부한 학습자가 민간 실무로 옮겨가도 부담이 없도록. 번역 시 공공 특수성(형평성·책임성·감사)이 민간 기법을 어떻게 변형시키는지(예: A/B 테스트 대신 섀도 배포)를 함께 서술한다.

## 2) 본문(원고) 작성 원칙
- 본문에는 **핵심 코드만 3~5줄** 넣고, 전체 실행 코드는 `practice/`에 둔다.
- 본문에서 전체 코드는 항상 아래 형태로 참조한다:
  `_전체 코드는 practice/chapter{N}/code/{파일명}.py 참고_`
- 숫자/표/성능 지표/산출물 목록은 **실제 실행으로 얻은 것만** 기재한다.
- `contents.md`의 "절 구성/강화 항목"을 충족했는지 항상 교차 점검한다.
- 불릿/표/체크리스트 사용은 허용/권장한다(강의 교재 모드 우선).

## 3) 실습 코드 작성 원칙
- 실습 코드는 `practice/chapter{N}/code/` 아래에 **단독 실행 가능**해야 한다.
- 크로스 플랫폼 경로: `pathlib.Path` 또는 `os.path.join()` 사용, 상대 경로 우선.
- 기본 실행 형태:
  ```bash
  cd practice/chapter{N}
  python3 -m venv venv && source venv/bin/activate
  pip install -r code/requirements.txt
  python code/{N}-{M}-{주제}.py
  ```
- 환경에 따라 `python`이 없을 수 있으므로, `python3`을 기준으로 안내한다.

## 4) "실제 실행"과 산출물 관리
- 변경한 실습 스크립트는 최소 1회 실제 실행하여 오류 없이 동작함을 확인한다.
- 실습 산출물은 기본적으로 `practice/chapter{N}/data/output/` 아래로 저장한다.
- 문서(`docs/ch{N}.md`)에는 산출물 경로를 일관되게 적고, 실제 생성 여부를 확인한다.

## 5) Word 변환 (출판 파이프라인)
- Markdown 원고는 최종본을 `docs/ch{N}.md`에 둔다.
- Word 변환은 `ms-word` 파이프라인을 사용한다:
  ```bash
  cd ms-word && npm install    # 최초 1회
  npm run convert:chapter {N}  # 개별 챕터
  npm run convert:all          # 전체 일괄
  npm run create:book          # 통합 도서
  ```
- 변환 결과는 `ms-word/output/ch{N}.docx`에 생성된다.

## 6) LLM 품질 검증 (선택)
- Multi-LLM 리뷰 스크립트로 챕터 검토를 수행할 수 있다:
  ```bash
  cd scripts
  python3 multi_llm_review.py --chapter {N}
  ```
- 결과 JSON은 `content/reviews/ch{N}_review_YYYY-MM-DD.json`에 저장된다.
- 리뷰의 "주요 이슈(critical_issues)"는 가능한 범위에서 원고/코드에 반영한다.

## 7) 진행 체크리스트 업데이트
- 원고/코드/그래픽/검토 진행상태는 `checklists/book-progress.md`에 반영한다.
- 표의 범례(R/W/C/G/V)를 유지하고, 작업 완료 시 해당 칸을 갱신한다.

## 8) 작업 후 최소 확인 (출력 기준)
- 수정한 `docs/ch{N}.md`가 `contents.md` 요구사항을 충족하는지 확인
- 본문에 들어간 결과값/파일 경로가 실제 실행/생성 결과와 일치하는지 확인
- 필요한 경우 Word 변환(`ms-word/output/ch{N}.docx`)까지 갱신

## 9) 학부 강의교재 파생 (`lecture/`)
- `docs/`의 실무교재 작성·검토가 끝난 뒤, 같은 장을 근거로 `lecture/`에 학부 3-4학년용 강의교재를 만든다.
- `lecture/` 원고는 실무교재의 내용을 대체하지 않는다. 원천 근거는 `docs/`, `practice/`, `content/research/`이며, 쉬운 설명·비유·직관·수업 활동으로 재구성한다.
- 학부용 원고는 복잡한 운영 세부사항을 줄이고, “왜 필요한가 → 일상적 비유 → 핵심 개념 → 작은 예 → 확인 질문” 순서를 우선한다.
- 실무교재에 없는 사실, 수치, 실행 결과를 새로 만들어 넣지 않는다. 추가 설명이 필요하면 근거 파일 또는 실제 실행 결과를 먼저 확보한다.
- 권장 출력 위치:
  - `lecture/chapter{N}.md`: 장별 학부 강의교재
  - `lecture/assets/`: 강의용 그림·도식
  - `lecture/slides/`: 필요 시 강의 슬라이드 초안

---

## 역할(스킬) 요약

이 프로젝트는 Codex CLI에서 역할별로 재사용 가능한 스킬을 사용한다.

| 스킬 | 역할 | 출력 위치 |
|---|---|---|
| `$book-planner` | 집필계획서 작성 | `schema/chap{N}.md` |
| `$book-researcher` | 자료 조사 | `content/research/` |
| `$book-writer` | 원고 초안 작성 | `content/drafts/` → `docs/` |
| `$book-coder` | 실습 코드 작성 | `practice/chapter{N}/code/` |
| `$book-reviewer` | 품질 검토 | `docs/ch{N}.md` (최종) |
| `$book-graphic` | 시각자료 제작 | `content/graphics/` |

## 계획 수립 (Codex Plan)

집필/대규모 수정 시작 시 `update_plan`으로 단계와 완료 조건을 명확히 둔다.

### 워크플로우
```
1. 요구사항 정리
   - contents.md에서 해당 장/절 요구사항 확인
   - 인접 챕터/스키마/초안 구조 확인
2. 작업 계획 수립 (update_plan)
   - 산출물(파일 경로)와 검증 기준 포함
3. 실행
   - 리서치/코드/원고/그래픽 순서로 진행(필요 시 병렬)
4. 검토 및 반영
   - 스키마/목차/실행 결과/참고문헌 교차 검증
5. 학부 강의교재 파생(실무교재 완료 후)
   - docs/chapter{N}.md 또는 docs/ch{N}.md를 근거로 lecture/chapter{N}.md 작성
   - 직관·비유·수업 질문 중심으로 재구성
```

### 사용 시점
- 새 챕터 시작 시 (필수)
- 목차 변경 시 (영향 분석)
- 대규모 수정 시 (계획 수립)

---

## 검증 하네스 & `.ai/` 메모리 (2026-08-15 갱신)

집필 운영 문서(위)에 더해, 실습코드 검증과 작업 메모리를 위한 보조 장치를 둔다.

### 하네스 체계 (2단 구조)

| 하네스 | 역할 | 성공 조건 | 언제 실행 |
|--------|------|-----------|-----------|
| `scripts/run_and_capture.py N` | **실행 증거 게이트** — 장별 코드 실행 + SHA-256 해시 기록 | 모든 파일 exit_code=0 | 코드 작성/수정 완료 후 |
| `scripts/harness.sh` | Lint + Test 게이트 — ruff·pytest 자동 감지 실행 | `HARNESS_PASS` 출력 | 코드 수정 후 |
| `scripts/verify.sh` | 빠른 점검 (lint + test) | `VERIFY_PASS` 출력 | 빠른 확인 시 |

### run_and_capture.py — 실행 증거 게이트 (2026-08-15 신규)

geoai 프로젝트에서 이식. 하네스 엔지니어링 원칙 "instructions decay, enforcement persists"의 구현.

```bash
# 10장 전체 코드 실행 + 증거 생성
python scripts/run_and_capture.py 10

# 특정 파일만
python scripts/run_and_capture.py 10 --file 10-1-model-serving.py

# 검증 모드: 캡처 이후 소스가 바뀌었는지 확인 (실행 안 함)
python scripts/run_and_capture.py 10 --verify
```

**산출물**: `practice/chapterN/results/{파일}.log` + `{파일}.evidence.json`

**핵심 규칙**:
- **본문의 실행 결과 수치는 `.log` 파일에서만 인용한다.** 로그 없는 결과 서술은 가짜로 간주.
- 코드 수정 후 `--verify`로 소스 해시 일치 여부 확인. 불일치 = 재실행 필요.
- 증거 JSON에 휘발성 식별자(run_id 등) 넣지 않는다 — 실행마다 바뀌어 diff를 만든다.

### harness.sh — Lint + Test 게이트

- 프로젝트 타입 자동 감지(Node/Python/Makefile) 후 install·lint·test·build 실행.
- 성공 시 `HARNESS_PASS` 출력. **실습코드를 수정했으면 실행하고, `HARNESS_PASS` 가
  나오기 전에는 "완료"라고 보고하지 않는다.**
- 빠른 점검은 `./scripts/verify.sh`(lint+test, `VERIFY_PASS`).

### .ai/ 작업 메모리

- `common.md`(작업 사이클·완료 기준) · `context.md`(현재 상태) · `todo.md`(작업 상태)
- 작업 시작 시 `context.md`→`todo.md`→`.claude/GOTCHAS.md` 순으로 읽고, 작업 끝에 `context.md`·`todo.md`를 갱신한다.

### .claude/ 지뢰 목록 & 교훈 로그

- **`.claude/GOTCHAS.md`** — 집필 전 필독. 코드 실행·환경·라이브러리·서식 함정.
- **`.claude/lessons-learned.md`** — 실패→규칙 래칫 로그. 3회 반복 시 GOTCHAS로 승격.
- 기존 `.ai/gotchas.md`는 레거시로 유지하되, `.claude/GOTCHAS.md`가 정본.

> 이 하네스는 **집필 절차(1)~검토(4))를 대체하지 않는다** — 코드·메모리 측면의 보조 게이트다.

---

**마지막 업데이트**: 2026-08-15
