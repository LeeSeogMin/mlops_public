# 실습 15.1 결정적 코어(15-1)와 검증기·오케스트레이터는 표준 라이브러리만 사용한다
#   (json, sqlite3, hashlib, math, re, unicodedata, datetime, pathlib, urllib).
# 외부 의존성이 없으므로 별도 설치 항목이 없다 — Python 3.10+ 이면 바로 실행된다.
# LLM 생성 스냅샷(15-2-llm-snapshot.py)도 urllib(표준 라이브러리)로 호출한다.
