# 제10장 모델 배포와 CI/CD

---

## [전반부] 이론 강의
---

### 학습 목표

- 배치 추론과 온라인 추론의 차이와 판단 기준을 이해한다
- 네 가지 배포 전략(일괄·blue-green·카나리·shadow)을 실패 반경으로 비교한다
- FastAPI+Docker로 서빙 API를 만들고, CI/CD 파이프라인의 기본 검증 단계를 이해한다
- 배포 전략·롤백 트리거·게이트 배치·기동 대기 시간을 요구사항에서 고르고 근거를 적는다

---

### 1. 배치 추론과 온라인 추론
#### 1-1. 예측은 언제 계산되는가

○ **배치 추론** — 예측을 미리 만들어 저장
  - 매일 밤 전체 지역구의 내일 민원 건수를 한꺼번에 예측 → 테이블에 적재
  - 낮의 조회는 계산이 아니라 저장된 값의 lookup

○ **온라인 추론** — 요청이 도착한 순간 계산
  - 이용자가 입력을 보내면 API가 모델을 실행해 그 자리에서 답을 돌려줌

#### 1-2. 선택 기준 — 두 질문

○ **요청 시점에만 알 수 있는 입력이 필요한가?**
  - 이용자가 방금 입력한 값·현재 위치 등 미리 알 수 없는 입력 → 배치로 불가

○ **예측이 소비될 지점을 미리 열거할 수 있는가?**
  - "서울 25개 자치구의 내일 예측"처럼 대상이 유한 → 미리 다 만들 수 있음
  - "임의의 입력 조합" → 미리 만들 수 없음

**표 10.1** 배치 추론과 온라인 추론 비교

| 비교 축 | 배치 추론 | 온라인 추론 |
|---|---|---|
| 계산 시점 | 정해진 주기(예: 매일 밤) | 요청 도착 순간 |
| 응답 지연 | 조회만 하므로 빠름 | 모델 실행 시간이 요청 경로에 들어옴 |
| 입력 조건 | 계산 시점에 아는 입력만 | 요청 시점의 입력 사용 가능 |
| 실패의 형태 | **낡은 예측을 서빙**(계산 실패가 조용히 지나감) | **응답 불가**(서버 장애가 즉시 드러남) |
| 인프라 | 스케줄러 + 저장소 | 상시 서버·API·확장 |

○ **실패의 형태가 가장 중요하다**
  - 배치: 밤 배치가 죽어도 낮의 조회는 어제 값을 아무 오류 없이 돌려줌(조용한 실패)
  - 온라인: 서버가 죽으면 모든 요청이 즉시 실패(시끄러운 실패)
  - 어느 쪽의 실패인지 알고 설계하는 것이 운영의 시작

○ **구분은 도메인을 가리지 않는다** — 민간 추천 서비스도 매일 밤 전체 사용자의 추천 후보를 미리 계산하고 낮에는 조회만 한다
  - 전 지역구 예측을 미리 만들어 두는 것과 구조가 같음. 다른 것은 규모
  - 대상이 수천만이면 요청마다 계산하지 않아 아끼는 몫이 커짐 → 규모가 클수록 배치 쪽으로 기움
  - 판단 기준(입력의 가용 시점·대상의 열거 가능성)은 규모와 무관

#### 1-3. 정직한 반대 논거

○ 일 단위 민원 건수 예측은 사실 배치로 충분
  - 대상(자치구) 열거 가능, 피처(전일 건수) 하루 한 번 확정
○ 실습이 온라인 API를 세우는 이유: 서빙 계약·컨테이너 포장·CI 게이트라는 **배포의 규율**이 온라인 서빙에서 전부 등장하므로 관찰 대상으로 선택
○ 거꾸로의 함정: 배치로 충분한 문제에 상시 API를 세우면 비용과 장애 지점을 스스로 늘리는 과잉 설계

---

### 2. 일괄, 카나리, shadow, blue-green 배포
#### 2-1. 배포 전략은 실패 반경의 제한 방식이다

○ 핵심 질문: "틀렸을 때 피해가 어디까지 번지는가(실패 반경, blast radius)"

○ **일괄(recreate)**: 구버전을 내리고 신버전을 올림
  - 실패 반경 100% — 신버전이 틀리면 전체 이용자가 영향을 받음

○ **blue-green**: 두 환경(blue=현재, green=신규)을 나란히 두고 트래픽 스위치를 넘김
  - 반경 100%이지만 **롤백(rollback)이 스위치 한 번** — 구환경이 살아 있음
  - 비용: 환경 2벌

○ **카나리(canary)**: 신버전에 트래픽 일부(예: 5%)만 보내고 이상 없으면 점증
  - 실패 반경이 5%로 시작
  - 비용: 트래픽을 비율로 가르는 라우팅 인프라 + 판정 지표

○ **shadow**: 실제 요청을 신버전에 복제하되 응답은 구버전 것만 사용
  - 실패 반경 **0** — 신버전이 무엇을 답하든 이용자에게 닿지 않음
  - 비용: 계산 자원 2배, "이용자 반응"은 관찰 불가

**표 10.2** 배포 전략 비교

| 전략 | 교체 방식 | 실패 반경 | 롤백 경로 | 추가 비용 |
|---|---|---|---|---|
| 일괄 | 전량 즉시 교체 | 100% | 재배포(느림) | 없음 |
| blue-green | 환경 2벌 + 스위치 | 100%(순간) | 스위치 되돌림(즉시) | 환경 2벌 |
| 카나리 | 트래픽 비율 점증 | 시작 비율만큼 | 비율 0으로 축소 | 라우팅 + 판정 지표 |
| shadow | 요청 복제, 응답 미사용 | 0 | 해당 없음 | 계산 2배 |

#### 2-2. 민간→공공 적용: 트래픽 분할에서 그림자 관찰로

○ **민간 방식**: 카나리(트래픽 점진 전환), A/B 릴리스(무작위 분할)
  - 같은 시각에 시민마다 다른 버전이 응답하는 구조
  - 민원 배정·복지 판정처럼 권리에 영향을 주는 결정에서 **형평성·법적 정당성 문제**

○ **공공 변형 — shadow**
  - 응답 경로를 champion 하나로 고정 → 모든 시민이 같은 대우를 받음
  - challenger는 같은 입력을 받아 **로그로만** 남김
  - 점진 확대 대신 **승인 게이트 + 검수 후 일괄 전환**

**표 10.2b** 민간 배포의 공공 적용

| 민간 방법 | 공공 변형 | 왜 변형하는가 |
|---|---|---|
| 카나리(트래픽 점진 전환) | 승인 게이트 + 검수 후 일괄 전환 | 중간 상태가 시민 간 대우 차이를 만듦 |
| A/B 릴리스(무작위 분할) | shadow(응답 고정 + 로그 관찰) | 시민 무작위 분할은 형평성 문제 |
| 점진 전환 후 자동 승격 | 사람 검수·승인 후 전환 | 공공 배포는 책임 주체와 승인 이력이 산출물 |

#### 2-3. shadow 실측

○ 입력: 전일 건수 9(강남구, 8장 온라인 스토어 실측값)
  - 이용자가 받은 응답: **6.0** (champion=v1, 평균 예측기)
  - 로그에만 남은 값: challenger **7.3636** (v2 선형회귀: 3.6818 + 0.4091×9 = 81/11)
  - 차이 1.36이 shadow가 만드는 관찰 자료 → 이용자 위험 0으로 후보를 재평가

#### 2-4. 두 개의 교체 층

○ **모델 층**: 별칭 이동(레지스트리에서 champion이 v1→v2로 옮겨짐)
○ **서비스 층**: 컨테이너 재기동·트래픽 전환(앱이 모델을 시작 시 1회 로드하므로 별칭 이동만으로는 반영 안 됨)
○ 두 층이 만나야 교체가 완성 — 롤백도 두 층(별칭 되돌림 + 이전 이미지 재기동)

#### 2-5. 롤백 트리거 — 경로가 있어도 기준이 없으면 되돌리지 않는다

○ 표 10.2의 "롤백 경로"는 되돌리는 **방법**만 답함
  - 사고가 커지는 자리는 방법이 없어서가 아니라 **언제 되돌릴지**를 정해 두지 않아서
  - 지표가 나빠지는 동안 "조금 더 보자"가 반복되고, 되돌림은 피해가 다 번진 뒤에 옴

○ **트리거는 세 조각** — 어떤 지표를 / 어느 임계에서 / 얼마의 관측 창으로
  - 셋 중 하나라도 비면 트리거가 아니라 각오
  - 서비스 층 지표: 오류율·응답 지연(즉시 관측) / 모델 층 지표: 예측 분포 이탈(늦게 드러남, 11장)
  - 이 장 범위에서 바로 쓰는 트리거: 배포 직후 스모크 — 입력 9에 6.0이 안 나오면 이전 이미지로 복귀

○ **임계와 창은 되돌림의 대가에서 역산**
  - 되돌림이 싸면(이전 이미지 재기동) 창을 짧게 → 빨리 되돌려도 손해가 적음
  - 되돌림이 비싸면(저장소 스키마 변경 동반) 창을 늘려 오탐을 줄임
  - 공공에서 한 조각 더: 자동으로 되돌릴지 사람이 판단할지, 되돌린 사실과 근거를 어디에 남길지

○ **민간은 되돌림 속도를 지표로 관리** — MTTR(평균 복구 시간), 변경 실패율(DORA 표준 지표)
  - blue-green·카나리를 선호하는 이유가 이 둘을 줄이기 때문
  - 공공에서 같은 값을 재는 목적은 다름 — 개발 조직의 성적표가 아니라 복구 시간 약속의 근거

---

### 3. FastAPI 모델 서빙
#### 3-1. API는 모델에 계약을 입힌다

○ **신원 응답(/health)**: 살아 있는가 + **무엇을 서빙 중인가**(모델 이름·버전·로드 출처)
○ **예측(/predict)**: 입력(지역구 코드, 전일 건수) → 예측값 + 모델 신원
○ **입력 검증(422)**: 스키마에 어긋난 입력은 모델에 닿기 전에 거절
  - `lawd_cd`는 5자리 숫자, `x_prev_count`는 0 이상 정수
  - pydantic 선언이 기계가 집행하는 스키마 계약

#### 3-2. 모델 로드는 시작 시 1회

○ FastAPI lifespan을 사용해 기동 시점에 로드
  - 요청마다 로드하면 로드 지연 + 레지스트리 장애가 요청 경로에 들어옴
  - "무겁고 위험한 일은 기동 시점에, 요청 경로에는 계산만"
  - 대가: 떠 있는 프로세스는 별칭 이동을 모름(10.2의 두 층 문제)

#### 3-3. 서빙 실측

**표 10.3** 서빙 API 실측

| 호출 | 응답(실측) | 검산 |
|---|---|---|
| GET /health | model_version=1, model_source=registry-alias, shadow_enabled=true | champion=v1(9장 판정과 일치) |
| POST /predict {lawd_cd: 11680, x_prev_count: 9} | **forecast=6.0** | v1은 평균 예측기 → 36/6=6.0 |
| POST /predict {x_prev_count: −1} | **422** (모델 미도달) | 음수 건수는 입력 불가 |
| (shadow 로그) | champion 6.0 / challenger **7.3636** | 81/11(9장 계수로 손 검산) |

#### 3-4. 핵심 코드

```python
model = mlflow.pyfunc.load_model("models:/complaint_daily_forecaster@champion")
mv = MlflowClient().get_model_version_by_alias("complaint_daily_forecaster", "champion")
app.state.identity = {"model_name": ..., "model_version": str(mv.version)}
```

_전체 코드는 practice/chapter10/code/10-1-model-api/app.py 참고_

---

### 4. Docker 이미지와 기본 테스트
#### 4-1. "내 컴퓨터에서는 됐는데"의 종결자

○ Docker 이미지 = 코드 + 의존성(버전 고정) + 모델 실물을 하나의 불변 단위로 포장
  - 9장 재현 4요소 중 "환경"이 문서가 아니라 실행 가능한 실물로 고정됨
  - "한 번 만들면 고치지 않고 통째로 교체" — 불변 서버(immutable server) 원칙

#### 4-2. 레지스트리 참조 vs 이미지 고정

**표 10.4** 두 가지 모델 로드 방식

| | 레지스트리 참조(호스트 실행) | 이미지 고정(컨테이너) |
|---|---|---|
| 모델 로드 | `models:/...@champion` — 로드 시점의 champion | `/app/model` — 빌드 시점의 champion 복사본 |
| 모델 교체 | 별칭 이동 + 재기동 | **새 이미지 빌드** + 배포 |
| 장점 | 교체가 가벼움(개발·실험에 적합) | 불변 배포 단위, 롤백은 이전 이미지 재기동, 레지스트리 장애와 무관 |
| 위험 | 기동 시점에 따라 다른 모델 로드 가능 | 빌드와 별칭 이동 사이의 시차 — 출처 기록 필수 |
| 신원의 근거 | 레지스트리 조회(model_source=registry-alias) | 빌드 시 동봉한 `export_info.json`(model_source=local-dir) |

○ **민간도 같은 문제를 다른 도구로 푼다** — 이미지 태그(tag)·배포 메타데이터에 커밋 해시·빌드 번호를 박음
  - 목적은 `export_info.json`과 같음("지금 도는 이미지가 어느 소스에서 나왔는가")
  - 다른 것은 빈도 — 하루 여러 번 배포하면 사람이 못 적으므로 파이프라인이 자동으로 남김
  - 공공은 그 기록 위에 사람이 읽는 절차가 한 겹 더(폐쇄망 반입 심사)

#### 4-3. 실제로 겪은 오류 — 게이트 전부 통과한 코드가 컨테이너에서 즉시 실패

○ 원인: 앱이 파일 위치 기준으로 경로를 계산 → 이미지 안에서는 디렉터리 구조가 다름
  - pytest 통과, 호스트 실행 통과 → **컨테이너에서만** IndexError로 즉시 실패
  - 저장소 배치 위에서 돌 때는 경로 가정이 항상 참이므로 두 게이트가 결함을 잡을 수 없었음
○ 교훈: **실행 환경이 달라야만 드러나는 결함이 존재** → 컨테이너 스모크는 독립 게이트

#### 4-4. 테스트를 층으로 쌓는다

○ **pytest(TestClient)**: 서버 없이 앱 객체 직접 호출 → 계약 위반을 초 단위로 잡음
○ **호스트 실행 스모크**: 실제 프로세스·HTTP → 기동 절차·포트·레지스트리 연결 결함
○ **컨테이너 스모크**: 실제 배포 환경 → 이미지 배치·의존성 누락·경로 가정 결함

○ **환경 동등성 실측**: 같은 입력에 호스트와 컨테이너가 같은 답을 내는가?
  - 호스트 6.0 = 컨테이너 6.0, 두 환경 모두 422 거절 동작 동일

#### 4-5. 기동 대기는 얼마로 잡는가

○ 실습의 감시 루프는 헬스체크를 **90초**까지 기다림(`run_chapter10.py`의 `wait_health`)
  - **아래에서 올라오는 힘**: 기동에 실제 걸리는 시간 — 모델 로드가 기동 시점 1회(10.3)이므로 그보다 넉넉해야 함
  - **위에서 내려오는 힘**: 실패를 알아채는 속도 — 값이 클수록 죽은 서버를 붙잡고 기다림
  - 두 힘이 반대 방향 → 값 하나로 풀리지 않음

○ **해법은 값을 늘리는 대신 판정을 하나 더 붙이는 것**
  - 프로세스·컨테이너 생존을 함께 확인 → 죽었으면 타임아웃 전에 즉시 실패
  - 앞의 오류에서 90초를 다 기다린 것은 값이 틀려서가 아니라 "죽음"과 "느림"을 구분하는 판정이 없어서
  - 운영 헬스체크도 같은 구조 — 확인 주기·응답 타임아웃·연속 실패 횟수를 함께 정해야 느린 응답 한 번으로 멀쩡한 서버를 내리지 않음

---

### 5. GitHub Actions 기반 검증 흐름
#### 5-1. CI는 새 검증이 아니라 "잊지 않는 검증"이다

○ 10.4까지 게이트는 이미 다 있음 — 문제는 **누가 매번 실행하는가**
  - 사람은 급하면 건너뜀 — "한 줄 고쳤는데 뭘"이 사고의 표준 서사
○ CI(Continuous Integration, 지속적 통합): 게이트 실행 주체를 사람 → 저장소로 이동
  - push가 곧 게이트 실행, 생략이라는 선택지가 구조적으로 사라짐

#### 5-2. 워크플로의 단계

| CI 단계 | 로컬 동일 단계 | 잡는 결함 |
|---|---|---|
| 의존성 설치 | venv + requirements | 의존성 선언 누락 |
| bootstrap 실행 | 시나리오 1 | 데이터 지문 불일치 |
| pytest | 시나리오 2 | 서빙 계약 위반 |
| docker build | 시나리오 4 전반 | 이미지 정의·포장 결함 |
| 컨테이너 스모크(6.0 확인) | 시나리오 4 후반 | 배치·환경 가정 결함 |

#### 5-3. 게이트를 어디에 둘 것인가

○ 실제 결정은 "무엇을 검증하는가"보다 **같은 검증을 어느 층에 두는가**
  - 앞에서 뒤로 갈수록 비쌈 — pytest는 초 단위, docker build·컨테이너 스모크는 이미지 빌드와 기동 시간
  - 뒤쪽을 덜어내면 10.4의 경로 가정 결함처럼 앞 층에서 원리적으로 안 드러나는 결함이 통과

○ **배치를 정하는 세 질문**
  - 이 결함이 앞 층에서 드러날 수 있는가 → 앞으로 당김(계약 위반은 pytest)
  - 뒤 층에서만 드러나는가 → 그 층에 게이트 상설(배치·환경 가정은 컨테이너)
  - 놓쳤을 때의 대가가 얼마인가 → 되돌리기 비싼 결함은 비용을 치르고 앞에 세움

○ **게이트를 늘리면 배포가 느려진다** — 배포 빈도(deployment frequency, DORA 지표)가 떨어짐
  - 민간: "빨리 내보내 얻는 이득 대 장애 비용"으로 계산
  - 공공: 검증을 건너뛰어 얻는 것은 담당자의 시간, 잃는 것은 "어떤 검증을 거쳤는가"에 답할 근거
  - → 게이트를 줄이는 판단은 속도 문제가 아니라 증거 문제. 줄였다면 무엇을 왜 줄였는지 검수 문서에 남김

#### 5-4. CI가 못 잡는 것

○ 모델 예측 품질이 데이터 변화로 낡아 가는 것(드리프트) → 모니터링(11장)의 일
○ 부하에서의 성능 → 12장의 일
○ "CI 통과"는 배포 가능의 **필요조건이지 충분조건이 아님**

#### 5-5. 배포 전 검수 체크리스트

**표 10.5** 배포 전 검수 체크리스트(실습 자동 생성, 실측 8항목 전항 통과)

| # | 항목 | 근거 |
|---|---|---|
| 1 | 자동 테스트 통과 | pytest 4건 |
| 2 | /health가 모델 이름·버전을 답하는가 | model_version=1 |
| 3 | 알려진 입력 → 알려진 출력 스모크 | 9 → 6.0 |
| 4 | 잘못된 입력 거절 | 422 |
| 5 | 컨테이너 = 호스트 동일 예측(환경 동등성) | 6.0 = 6.0 |
| 6 | 모델 실물 이미지 고정 + 출처 동봉 | export_info.json |
| 7 | 레지스트리 버전 보존(롤백 전제) | v1·v2 보존, challenger 별칭 |
| 8 | 검증 파이프라인 정의(CI 워크플로) | ch10-model-api.yml |

#### 5-6. 역방향 — 이 장의 배포 규율을 민간 현장으로

○ **이커머스 추천 모델 교체** — shadow가 놓였던 자리에 카나리가 들어감
  - 새 모델을 전체 사용자의 1~5%에 먼저 노출 → 클릭률·전환율이 유의하게 높으면 10% → 50% → 100%
  - 나빠지면 비율을 0으로(표 10.2의 "비율 0으로 축소"가 실시간으로 작동)
  - 바뀐 것은 전략이지 판단 구조가 아님 — 롤백 트리거는 여기서도 지표·임계·관측 창 세 조각

○ **핀테크 결제 API 교체(blue-green)** — 구환경(blue)·신환경(green)을 나란히, 로드 밸런서 타겟 그룹 전환
  - 문제 시 스위치를 되돌려 분 단위 복귀 — "이전 이미지 재기동"이 "스위치 되돌림"이 됨
  - 롤백 목적지를 살려 두는 것이 롤백의 전제라는 성질은 같고, 대상이 이미지에서 환경 한 벌로 커진 것

○ **롤백 자동화** — 배포 후 일정 시간 오류율·지연을 자동 감시, 임계 초과 시 사람 개입 없이 이전 버전으로
  - "CI 통과는 필요조건이지 충분조건이 아니다"가 이 장치의 존재 이유
  - 공공은 방아쇠를 자동으로 할지 사람이 당길지가 별도 결정, 어느 쪽이든 되돌린 근거를 기록으로

○ **관통하는 하나** — 배치/온라인 선택, 배포 전략, 롤백 트리거와 목적지, CI 게이트 배치. 이 네 결정은 어느 현장에서도 똑같이 내린다. **근거만 도메인이 바꾼다**(공공: 형평성·감사·법적 의무 / 민간: 매출·비용·SLA)

○ **방향은 비대칭** — 민간→공공은 증거와 승인 이력이 한 겹 얹히지만, 공공→민간은 그 겹을 벗겨도 손해가 없다(신원 응답 /health → 장애 대응 시간 단축, 출처 동봉 이미지 → 사고 추적의 출발점, 자동 생성 체크리스트 → 배포 빈도를 올리면서 변경 실패율을 누름)

---

## [후반부] 실습
---

### 실습 10.1: 모델 API 컨테이너 빌드와 호출

**진행 순서: 설계 → 실행 → 대조 → 위험 식별**

남이 정해 둔 배포 방식으로 명령부터 실행하면 왜 shadow인지, 무엇이 나빠지면 되돌리는지를 묻지 않게 된다. 먼저 자기 판단으로 배포 설계를 정하고 결과를 예측한 다음 실행 로그와 맞춰 본다. 검수·감사는 마지막 4단계에서 다룬다 — 설계 근거와 실행 증거가 둘 다 있어야 검수 질문에 답할 수 있기 때문이다.

#### 실습 목표
- 배포 전략·롤백 트리거·게이트 배치·기동 대기 시간을 **스스로 정하고 근거를 적는다**(설계)
- 9장 champion 별칭을 FastAPI 서빙이 로드하고, shadow(challenger)를 로그로 관찰한다
- 같은 API를 pytest → 호스트 → 컨테이너의 층으로 검증하고, 환경 동등성(같은 입력→같은 예측)을 실측한다
- 환경 동등성을 실행 전에 예측하고 실측과 대조해 어긋난 지점을 설명한다(대조)

#### 1단계 설계 — 배포 설계표와 환경 동등성 예측

**요구사항(가상 발주 조건)**
- 자치구 담당자 화면 뒤에 일일 민원 건수 예측 API를 놓는다. 이용자는 25개 자치구 담당 공무원, 예측값은 인력 배치 참고 자료
- 새 후보 모델(9장 v2)이 있고, 검수자는 승인 전에 실요청 위에서 champion과 비교하고 싶어 한다
- 발주처 요구: 같은 시각에 담당자마다 다른 모델이 응답하는 상태를 만들지 않는다
- 운영망은 폐쇄망, 이미지는 반입 심사를 거친다. 반입 후 문제가 나면 되돌릴 수 있어야 한다
- 담당자가 코드 한두 줄을 고쳐 재배포하는 일이 잦다

**표 10.6** 배포 설계표 — 학습자 작성용 (본문 표 10.2·10.5는 예시이므로 베끼지 않는다)

| 항목 | 내가 정한 값 | 근거(요구사항의 어느 문장에서 나왔는가) |
|---|---|---|
| 배포 전략(일괄·blue-green·카나리·shadow) | | |
| 롤백 트리거 — 지표·임계·관측 창 | | |
| 롤백 목적지 | | |
| CI 게이트에 넣을 단계와 순서 | | |
| 기동 대기(헬스체크) 타임아웃 | | |
| 이 설계가 깨지는 조건 | | |

작성 규칙 셋 — ① 근거 칸에 "기본값이라서"를 쓰지 않는다 ② 롤백 트리거는 지표 하나만 적지 말고 임계와 관측 창을 함께 적는다 ③ "깨지는 조건"에는 자기 선택이 불리해지는 상황을 적는다(shadow면 계산 자원 2배, 게이트 넷이면 한 줄 수정에도 매번 이미지 빌드).

**환경 동등성 예측**: 스크립트는 같은 서빙 코드를 호스트(8010)와 컨테이너(8011)에서 띄우고 같은 입력을 보낸다. 실행하기 **전에** 적는다.

| 질문 | 내 예측 | 실제(3단계 기입) |
|---|---|---|
| 두 환경의 예측값이 같겠는가 | | |
| 다르다면 무엇 때문이겠는가 | | |
| /health의 `model_source`가 두 환경에서 같겠는가 | | |

#### 2단계 실행
#### 실행 환경
- Python 3.10 이상(3.13에서 검증)
- Docker 데몬(28.x에서 검증) — 없으면 `--skip-docker`로 시나리오 1~3까지 완주 가능

#### 실행 명령

```bash
cd practice/chapter10
python3 -m venv venv && source venv/bin/activate
pip install -r code/requirements.txt
python run_chapter10.py            # Docker 없이: python run_chapter10.py --skip-docker
```

#### 시나리오 구성

| 시나리오 | 내용 | 대응 절 |
|---|---|---|
| 1 | bootstrap — 레지스트리 재구성(지문 확인), champion export | 10.3, 10.4 |
| 2 | pytest — TestClient로 서빙 계약 4건 검증 | 10.3, 10.4 |
| 3 | 호스트 uvicorn(8010) — /health·/predict·422, shadow 로그 관찰 | 10.2, 10.3 |
| 4 | docker build → run(8011) → 동일 호출 → 환경 동등성 확인 | 10.4, 10.5 |

#### 핵심 코드

```python
model = mlflow.pyfunc.load_model(uri)              # 컨테이너: uri="/app/model"
info = json.loads(info_path.read_text(encoding="utf-8"))
return model, {"model_name": info["model_name"],
               "model_version": str(info["champion_version"]),
               "model_source": "local-dir"}
```

_전체 코드는 practice/chapter10/code/10-1-model-api/ 참고(app.py·bootstrap_registry.py·test_app.py·Dockerfile)_

#### 실행 결과 확인 사항

1. **시나리오 1**: 지문 `96f6b6b3ce9dcdd1…` 출력, export 로드 검증 예측=6.0
2. **시나리오 2**: pytest 4 passed
3. **시나리오 3**: health에 model_version=1, model_source=registry-alias, shadow_enabled=true
   - predict(전일 9) → 6.0, 잘못된 입력 → 422
   - shadow 관찰: champion_forecast=6.0, challenger_forecast=7.3636
4. **시나리오 4**: health의 model_source=local-dir, 예측 6.0(호스트와 동일)
5. **최종 출력**: `CH10_RUN_PASS` (Docker 미사용 시 `CH10_RUN_PARTIAL`)

#### 3단계 대조 — 예측과 실측 맞춰 보기

예측표의 "실제" 칸을 `ch10_deploy_report.json`의 `consistency`·`host`·`container` 값으로 채운다.

| 질문 | 실측 |
|---|---|
| 두 환경의 예측값 | 호스트 6.0 = 컨테이너 6.0 |
| /health의 `model_source` | 호스트 registry-alias / 컨테이너 local-dir |
| 잘못된 입력 | 두 환경 모두 422 |

○ **"같다"로 적었다면 근거가 무엇이었나** — 값이 같은 것은 세 가지를 함께 고정했기 때문
  - 모델 실물(이미지에 구움) + 의존성 버전(`requirements.txt` 고정) + 입력 계약(pydantic 스키마)
  - 하나만 흔들려도 값은 갈릴 수 있음. "같은 코드니까"는 근거가 아님 — 코드는 셋 중 하나일 뿐

○ **"다르다"로 적었다면 무엇을 걱정했나** — 그 걱정이 곧 실제 위험 목록
  - 의존성 버전이 이미지와 호스트에서 갈리는 경우
  - 빌드 시점의 champion과 지금 별칭이 가리키는 버전이 어긋나는 경우(표 10.4의 "시차")
  - 이번에 안 나타난 이유는 막는 장치를 넣어 두었기 때문

○ **`model_source`가 갈렸다는 것의 뜻** — 같은 답을 냈지만 출처를 서로 다른 곳에서 증명
  - 호스트는 레지스트리 조회, 컨테이너는 동봉 `export_info.json`
  - "같은 답이 나오는가"와 "무엇을 서빙 중인지 답할 수 있는가"는 별개 질문. 검수는 둘 다 물음

○ **놓치기 쉬운 것** — 환경 동등성은 두 환경이 **모두 떴을 때만** 물을 수 있는 질문
  - 10.4의 경로 가정 결함은 값이 갈리는 실패가 아니라 컨테이너가 아예 응답 못 하는 실패
  - 예측표를 값 비교로만 채웠다면 그 실패 유형을 통째로 빠뜨린 것

→ 표 10.6의 "이 설계가 깨지는 조건" 칸을 지금 다시 쓴다. **제출하는 설계표는 이 수정을 반영한 최종본이다.**

#### 관찰 과제

○ **관찰 1**: "6.0이 나오는가"라는 한 질문이 네 층을 관통
  - bootstrap export 검증 → pytest → 호스트 → 컨테이너
  - 손 검산 가능한 스모크 기준의 힘 — 6쌍 극소 데이터 설계가 배포 장에서도 유효

○ **관찰 2**: 층마다 잡는 결함이 다르다
  - 경로 가정 결함은 pytest·호스트를 통과하고 컨테이너에서만 발현
  - 층을 생략했다면 "배포 후 장애"로 발견됐을 것

○ **관찰 3**: 같은 서빙 코드, 두 신원 근거
  - 호스트: registry-alias / 컨테이너: local-dir
  - 개발의 유연함(별칭 참조)과 배포의 불변성(이미지 고정)은 로드 URI 하나의 차이

#### 산출물

| 산출물 | 내용 |
|---|---|
| `ch10_deploy_report.json` | bootstrap·테스트·호스트·컨테이너·shadow 실측의 증거 파일 |
| `ch10_release_checklist.json` | 배포 전 검수 체크리스트(정본, 실측 자동 생성) |
| `ch10_bootstrap_summary.json` | 재구성 판정·export 출처 |
| `ch10_shadow_log.jsonl` | shadow 관찰 로그(요청별 champion/challenger) |
| 표 10.6 배포 설계표(직접 작성) | 배포 전략·롤백 트리거·게이트 배치·기동 대기와 근거 — 검수 자산, 3단계 대조 후 확정 |

#### 4단계 위험 식별 — 설계와 증거를 검수 언어로

검수·감사가 요구하는 형태는 "무엇이 잘못될 수 있는가 / 근거가 어느 산출물의 어느 값인가 / 무엇으로 막는가"다. 앞 네 행은 실행 증거에서, 마지막 행은 1단계 설계에서 나온다.

| 위험 | 실습 근거 | 대응 방향 |
|---|---|---|
| 환경 가정 결함의 배포 유출 | 경로 계산 IndexError — 컨테이너 스모크에서만 발현 | 테스트 층화 + 컨테이너 게이트 상설 |
| 어느 모델이 서빙 중인지 불명 | /health 신원 + export_info 출처 동봉 | 신원 없는 서빙 API를 검수에서 반려 |
| 검증 생략 배포 | 체크리스트 8항목 자동 생성 + CI 워크플로 | 게이트 실행 주체를 사람에서 저장소로 |
| 실패 잔해 소실로 진단 불가 | --rm이 크래시 로그를 삭제(실제 경험) | 로그 보존 기동 + 명시적 정리 |
| 되돌릴 기준 없이 배포 | 1단계 설계: 트리거의 임계·관측 창을 비운 채로도 실행이 끝난다 | 지표·임계·관측 창 명시, 롤백 목적지 보존 확인 |

대응 방향에 "모니터링한다"만 적으면 그 행은 검수 문서에서 쓸모가 없다. **바꿀 설정값이나 바꿀 설계 결정**을 적는다.

---

### 과제 (LMS 제출)

#### 제출물
다음 두 가지를 LMS에 업로드한다.

1. **배포 설계표**(표 10.6 최종본, 3단계 대조 반영) — 텍스트·이미지 어느 형식이든 무방
2. `practice/chapter10/data/output/ch10_deploy_report.json` — 실습 10.1을 실행해 생성된 JSON 파일 그대로

#### 평가 기준
- 두 파일을 모두 제출하면 완료로 인정한다(별도 배점 없음).
- 설계표는 값의 정답 여부가 아니라 **근거 칸이 채워져 있는지**를 본다 — "기본값이라서"는 근거가 아니다.

#### 마감
- 수업일 포함 7일 이내

---
