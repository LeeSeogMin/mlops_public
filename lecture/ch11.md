# 제11장 모니터링과 드리프트 관리

---

## [전반부] 이론 강의 (50분)

---

### 학습 목표

- 시스템 지표와 모델 지표를 구분하고, 각각이 잡는 고장의 형태를 설명한다
- 데이터 드리프트(P(X)의 변화)와 컨셉 드리프트(P(Y|X)의 변화)의 차이를 설명한다
- 드리프트 지표(KS·PSI·KL)의 적용 조건을 알고, 재학습 트리거 조건을 설계한다

---

### 1. 시스템 모니터링: 지연, 처리량, 오류율 (13분)

#### 1-1. 감시의 1층 — 서비스가 살아 있는가

○ **핵심 전제**
  - 모델이 틀리기 전에 서비스가 먼저 죽는 경우가 더 잦다
  - 응답이 안 오는 API 앞에서 예측 품질은 논점이 아니다
  - 감시의 1층은 모델과 무관한 세 질문으로 시작한다

○ **골든 시그널(Golden Signals, Google SRE)**
  - Google SRE 팀이 분산 시스템에서 먼저 볼 지표를 네 가지로 압축한 것
  - 지연(latency), 트래픽(traffic), 오류(error), 포화(saturation)
  - 이 네 지표의 특징: 원인(CPU 사용률 등)이 아니라 이용자가 겪는 증상을 먼저 잡는다
  - 이 장은 앞의 셋을 다루고, 넷째(포화)는 12장에서 다룬다

#### 1-2. 세 지표

○ **지연(latency)** — 요청이 얼마나 걸리는가
  - 평균의 함정: 4장 Kafka 실측에서 평균 3.5ms, 최대 110.7ms(첫 레코드, 콜드스타트(cold start))
  - 평균만 보면 "3.5ms짜리 서비스"이지만 첫 요청자는 30배를 기다렸다
  - 운영 감시는 평균이 아니라 **분위수**(p50, p95, p99)를 본다
  - 오류 요청의 지연을 따로 추적해야 한다 — 즉시 실패하는 요청이 섞이면 평균 지연이 좋아 보이는 착시가 생긴다

○ **처리량(throughput)** — 얼마나 많이 오는가
  - 초당 요청 수, 분당 건수로 측정
  - 급증만 신호가 아니다 — 급락도 신호다
  - 민원 접수가 평소의 1/10로 떨어졌다면 → 민원이 줄어든 것이 아니라 상류(접수 채널·수집 파이프라인)가 죽었을 가능성이 먼저다
  - 7장에서 입력 파일이 없던 날 배치가 실패로 전파되던 것과 같은 구조

○ **오류율(error rate)** — 얼마나 실패하는가
  - 4xx(호출자 쪽 문제)와 5xx(서비스 쪽 문제)를 분리해 센다
  - 10장 실측: 잘못된 입력(x_prev_count=-1) → 422로 거절 — 이것은 계약의 정상 작동
  - 오류율 0이 목표가 아니다: 잘못된 입력은 거절되어야 정상
  - 422가 갑자기 급증하면 → 호출자들이 일제히 틀리기 시작했다는 뜻 → 상류의 스키마 변경(2장)을 의심

#### 1-3. 시스템 지표 vs 모델 지표

**표 11.1** 시스템 지표와 모델 지표의 구분

| 비교 축 | 시스템 지표(1층) | 모델 지표(2층) |
|---|---|---|
| 답하는 질문 | 서비스가 살아 있는가 | 서비스가 옳은 답을 내는가 |
| 대표 지표 | 지연 p95, 처리량, 오류율(4xx/5xx) | 입력 분포, 예측 분포, (지연된) 성능 |
| 고장의 형태 | 응답 불가·느림·5xx — 시끄럽다 | 200 OK인데 틀린 답 — 조용하다 |
| 신호의 속도 | 즉시(요청 단위) | 느림(분포는 누적, 성능은 레이블 대기) |
| 주 대응 | 인프라 점검·롤백(rollback)·부하 점검 | 원인 분류·재학습 판단 |

#### 1-4. 알림을 누가 받는가 — 수신 구조와 알림 피로

○ **민간**: 24/7 on-call 로테이션
  - 한 엔지니어가 정해진 기간 모든 경보를 받는다
  - PagerDuty·Opsgenie 같은 인시던트(incident) 도구에 심각도(severity)별 상신 경로를 규칙으로 등록해 두면, 경보가 뜰 때 그 규칙대로 호출이 나간다

○ **공공**: 당직 체계 + 결재선 분리
  - 즉시 조치(서버 재기동 등)는 당직 교대에 붙인다
  - 판단이 필요한 조치(재학습 승인·시민 공지·정책 대응)는 직제상 결재선에 붙인다
  - 경보가 특정 당직 시간대에 몰리면 그 근무자만 소진되고, 책임이 여러 부서에 흩어지면 아무도 대응하지 않는 공백이 생긴다

○ **알림 피로(alert fatigue)** — 도메인 공통
  - 헛경보가 쌓이면 담당자가 경보를 읽지 않게 된다
  - 경보를 늘리는 쪽보다 줄이는 쪽이 안전할 때가 있다
  - 임계는 상수가 아니라, 오경보 이력과 함께 정기적으로 다시 세우는 운영 파라미터
  - 실습의 "주의 등급에는 사람 호출을 붙이지 않는다"가 이 원칙의 축소판

---

### 2. 모델 모니터링: 예측 분포, 성능, 신뢰도 (10분)

#### 2-1. "200 OK인데 틀린 답" — ML 서비스 고유의 고장

○ 시스템 지표가 전부 정상인 채로 모델이 틀리기 시작한다
  - 서버는 빠르고, 오류율은 0이고, /health는 ok인데 예측이 세상과 어긋난다
  - 코드가 그대로인데 세상이 변했기 때문이다
  - ML 시스템의 유지 부담은 코드가 아니라 데이터 의존성과 세상의 변화에서 온다(Sculley et al., 2015)

#### 2-2. 레이블 지연 — 성적표는 하루 늦게 온다

○ 정공법은 예측과 정답을 대조해 MAE를 계산하는 것
  - 문제: 정답(레이블)이 늦게 온다
  - 민원 건수 예측의 정답은 다음 날이 되어야 확정 — 오늘의 성능을 오늘 알 수 없다
  - 레이블 지연이 며칠~몇 주인 과제(심사 결과, 재방문 여부)도 흔하다

○ 모델 모니터링은 도착 순서대로 층을 이룬다
  - **지금 볼 수 있는 것**: 입력 분포, 예측 분포, 신뢰도
  - **레이블 도착 후**: 지연된 성능 평가로 확정 판정

○ **레이블이 언제 오는가가 1차 신호를 정한다**
  - 즉시(초 단위): 민간 추천의 CTR(Click-Through Rate, 클릭률) — 클릭이 곧 레이블이라 CTR 하락 자체가 1차 신호
  - 하루: 민원 건수 예측 — 입력 분포를 먼저 보고, 밤에 레이블이 오면 성능으로 확정
  - 수일~수개월: 금융 사기 탐지 — 그동안 입력 분포가 유일하게 볼 수 있는 신호
  - 설계의 첫 질문은 "어떤 지표를 볼까"가 아니라 "이 과제의 레이블은 언제 도착하는가"다

#### 2-3. 레이블 없이 보는 세 가지

○ **입력 분포**: 모델에 들어오는 값들의 분포가 학습 당시와 같은가
  - 데이터 드리프트 감시(11.3) — 레이블 없이 가장 먼저 가동 가능
  - 이 장의 실습이 바로 이것이다

○ **예측 분포**: 모델이 내놓는 값들의 분포가 평소와 같은가
  - 응답 로그만으로 계산 가능
  - 한계: 9장 champion(v1)은 평균 예측기라 입력이 무엇이든 6.0을 답한다 → 예측 분포가 상수 → 세상이 변해도 변하지 않는다
  - 입력에 둔감한 모델일수록 입력 분포를 직접 봐야 한다

○ **신뢰도**: 분류 모델이 확률 출력을 낸다면
  - 확신에 찬 예측(0.9 이상)의 비중이 줄고 경계 사례(0.5 부근)가 늘면 → 모델이 낯선 입력을 만나고 있다는 뜻
  - 이 교재의 회귀 모델은 불확실성을 출력하지 않으므로 이 신호를 쓸 수 없다

#### 2-4. 10장 shadow 로그 — 감시 자료의 원형

○ 10장 실측: 같은 입력(강남구, 전일 건수 9)에 대해 champion 6.0, challenger 7.3636
  - 관측 1건은 분포가 아니다
  - 운영에서 이 로그가 쌓이면 두 모델의 **예측 분포 비교**가 된다
  - 레이블이 도착하면 **지연된 성능 비교**가 된다

#### 2-5. 모니터링 지표 설계표

**표 11.2** 모니터링 지표 설계표(민원 예측 서비스 기준, 발췌)

| 지표 | 층 | 기준선 | 경보 시 1차 대응 |
|---|---|---|---|
| 응답 지연 p95 | 시스템 | 배포 직후 안정 구간 | 인프라·부하 점검(12장) |
| 처리량(분당 요청) | 시스템 | 요일·시간대 패턴 | 급락이면 상류 생존 확인 |
| 오류율(4xx/5xx) | 시스템 | 평시 비율 | 5xx는 서비스, 422 급증은 상류 스키마 의심 |
| 입력 분포 | 모델 | 학습 데이터 분포 | 원인 분류 착수(11.4) |
| 예측 분포 | 모델 | 초기 관찰 창 | challenger 재심 평가(10.2) |
| 지연된 성능(MAE) | 모델 | 승격 당시 지표(9장) | 재학습 판단표 가동(11.4) |

○ **민간은 여기에 층을 하나 더 쌓는다** — 시스템 + 모델 + 사업 지표(매출·전환·이탈)
  - 층을 나란히 두는 목적: 원인 가르기 — 매출 하락이 모델 노후 때문인지 시장 변화 때문인지는 두 층을 함께 읽어야 갈린다
  - 공공의 셋째 층 대응물: 민원 처리 지연, 미결 적체, 지원사업 집행률 — "서비스가 시민에게 도달했는가"에 답한다
  - 무게중심의 차이: 공공은 입력 데이터 분포가 1차 감시 대상, 민간은 수익 지표 변화가 먼저이고 입력 분포는 원인을 캘 때 연다

---

### 3. 데이터 드리프트와 컨셉 드리프트 (14분)

#### 3-1. 두 가지 방식으로 낡는다

○ **데이터 드리프트**: 입력의 분포 P(X)가 변한다
  - 예: 휴가철이 되어 지역구별 민원 건수 자체가 커짐
  - 모델이 받는 질문의 분포가 변했다
  - 관계 P(Y|X)는 그대로일 수도 있다
  - **레이블 없이 감지 가능** — 오늘 당장 가능

○ **컨셉 드리프트**: 입력과 정답의 관계 P(Y|X)가 변한다
  - 예: 새 제도 시행으로, 같은 "전일 9건"이라도 다음 날 기대 건수가 달라짐
  - 질문은 같은데 정답이 변했다
  - **원칙적으로 레이블이 있어야 확인** — 레이블 지연이 병목

○ **두 가지 함정**
  - 입력 분포가 그대로여도 컨셉은 변할 수 있다 → 입력 감시만으로는 보이지 않는 사각
  - 입력 분포가 변해도 성능은 멀쩡할 수 있다 → 모델이 둔감한 방향의 변화라면 그렇다
  - 그래서 드리프트 알림은 "성능 저하 확정"이 아니라 **"확인 착수"의 신호**로 설계한다

**표 11.3** 데이터 드리프트와 컨셉 드리프트

| 비교 축 | 데이터 드리프트 | 컨셉 드리프트 |
|---|---|---|
| 정의 | 입력 분포 P(X)의 변화 | 관계 P(Y\|X)의 변화 |
| 감지 재료 | 입력 로그만으로 가능 | 원칙적으로 레이블 필요 |
| 감지 시점 | 즉시(누적되는 대로) | 레이블 지연만큼 늦음 |
| 성능과의 관계 | 저하 "가능성"의 신호 | 저하의 직접 원인 |
| 대표 대응 | 원인 분류 → 필요 시 재학습 | 재학습(관계 자체를 다시 배움) |

○ **같은 구분이 민간에서도 그대로 성립한다** — 이탈 예측의 예
  - 경쟁 서비스 등장·요금 정책 변경 → 행동 피처(로그인 빈도·사용 시간)가 그대로여도 이탈 여부와의 관계가 달라진다 = 컨셉 드리프트
  - 신규 이용자 유입으로 행동 피처의 분포 자체가 이동 = 데이터 드리프트, 레이블 없이 곧바로 잡힌다
  - 조치도 갈린다: 앞은 관계를 다시 배우고, 뒤는 먼저 원인을 가린다

#### 3-2. 변화의 시간적 양상

○ 네 유형(Lu et al., 2019)
  - **급격(sudden)**: 정책 시행일에 계단식 변화
  - **증분(incremental)**: 여러 중간 상태를 거쳐 새 분포로 이동
  - **점진(gradual)**: 옛 분포와 새 분포가 한동안 번갈아 나타남
  - **재발(recurring)**: 계절처럼 주기적으로 돌아옴
  - 증분과 점진은 운영 대응이 사실상 같아 묶어 다룬다

○ **재발형이 특히 중요하다**
  - 매년 여름의 변화를 매년 "이상"으로 경보하면 알림 피로만 쌓인다
  - 경보로 소진하지 말고 계절 피처로 모델에 흡수시키거나 임계를 계절에 맞춰 조정한다

#### 3-3. 지표 3종 — 같은 질문, 다른 성격

○ **KS 2표본 검정(Kolmogorov-Smirnov test)**
  - 두 표본의 누적분포 최대 간격(통계량 D)을 재고, p-value를 답한다
  - "같은 분포에서 나왔다"는 가설 아래 그 간격이 우연히 나올 확률
  - 분포 가정 없음, 연속형에 적합
  - 주의: 표본이 크면 사소한 차이도 유의하게 나오고, 작으면 진짜 변화도 놓친다

○ **PSI(Population Stability Index, 모집단 안정성 지수)**
  - 기준 분포를 분위수 구간으로 자르고, 구간별 비율의 이동을 합산
  - PSI = Σ (현재%−기준%)·ln(현재%/기준%)
  - 신용평가 업계에서 모집단 안정성 점검용으로 정착
  - 관행 임계: 0.1(주의), 0.25(경보) — 출발점이지 법칙이 아니다(Yurdakul, 2018)

○ **KL divergence(Kullback-Leibler divergence)**
  - 같은 구간 확률로 KL(P‖Q) = Σ p·ln(p/q)를 계산
  - 비대칭: 방향에 따라 값이 다르다(기준→현재 ≠ 현재→기준)
  - 상대 확률이 0인 구간에서 발산 → 스무딩(smoothing)이 필수
  - PSI와의 관계: **PSI = KL(기준‖현재) + KL(현재‖기준)** — PSI는 KL의 대칭화(양방향 합)

**표 11.4** KS·PSI·KL의 적용 조건

| 항목 | KS 검정 | PSI | KL divergence |
|---|---|---|---|
| 답의 형태 | p-value(우연인가) | 거리(얼마나 큰가) | 거리(방향 있음) |
| 구간화 | 불필요 | 필요(기준 분위수 권장) | 필요(동일 구간) |
| 표본 크기 민감성 | 크면 과민, 작으면 둔감 | 구간당 빈도 작으면 불안정 | PSI와 동일 + 0빈도 발산 |
| 관행 임계 | 유의수준(예: 0.05) | 0.1 / 0.25(업계 관행) | 합의된 관행 없음 |

○ 검정과 거리는 다른 질문에 답한다
  - KS: "우연으로 보기 어려운가"
  - PSI: "이동의 크기가 얼마인가"
  - 대표본이면 KS만으로 전부 "유의" → 거리 지표가 실질 판단을 맡는다
  - 소표본이면 거리 지표가 출렁여 검정이 과속 방지턱이 된다
  - 알림 규칙이 둘을 함께 요구하는 이유

○ **무엇의 분포에 지표를 걸 것인가도 설계다**
  - 이 장의 실습: 입력 변수(PM10)의 분포에 KS·PSI를 건다
  - 같은 계산을 **예측 오차의 분포**에 걸 수도 있다 — 민간 수요 예측이 그렇게 쓴다(재고 과잉·품절을 가르는 것은 예측값이 아니라 오차)
  - 오차 분포의 이동은 입력 분포가 그대로여도 나타난다 → 레이블이 도착하는 과제에서 컨셉 드리프트의 신호
  - 공공 적용: 민원 건수 예측의 일별 오차를 쌓아 분포로 감시하면, 표 11.2의 지연된 성능이 단일 MAE에서 분포로 넓어진다

---

### 4. 재학습 판단 기준 (13분)

#### 4-1. 알림은 질문이지 명령이 아니다

○ 드리프트 알림이 떴다고 재학습 버튼을 바로 누르지 않는다
○ 알림과 조치 사이에 두 확인이 있어야 한다

○ **첫째 — 원인 분류**
  - 2장 분류: 정책·계절·이벤트·시스템 변경
  - 운영에서 추가: **데이터 결함** — 상류 파이프라인이 깨져 단위가 바뀌거나 결측이 급증한 경우
  - 깨진 데이터로 재학습하면 깨진 세상을 학습한 모델이 나온다 → 최악의 수
  - 계절 재발이면 재학습 대신 임계 보정·계절 피처 추가가 답일 수 있다
  - 진짜 세상의 변화(정책 시행, 구조적 수요 변화)일 때에만 재학습이 후보가 된다

○ **둘째 — 성능 확인**
  - 입력 분포 변화가 성능 저하를 보장하지 않는다
  - 레이블이 도착하면 지연된 성능 평가로 저하를 확인한 뒤 움직인다

#### 4-2. 재학습 트리거 판단표

**표 11.5** 재학습 트리거 판단표

| 드리프트 알림 | 성능 저하(레이블 확인) | 원인 분류 | 조치 |
|---|---|---|---|
| 정상 | — | — | 기준선 유지, 정기 감시 |
| 주의 | 미확인(레이블 대기) | 착수 | 감시 강화, 로그 축적 — 사람 호출 없음 |
| 경보 | 확인됨 | 데이터 결함 | **수리**(재학습 금지) |
| 경보 | 확인됨 | 계절 재발 | 임계 보정·계절 피처 검토 |
| 경보 | 확인됨 | 실제 변화(정책 등) | **재학습** → challenger 등록 → 9장 게이트 |
| 경보 | 저하 없음 | 실제 변화 | 감시 지속 — 모델이 둔감한 방향의 변화, 재학습 보류 가능 |

#### 4-3. 트리거 방식: 정기, 이벤트, 혼합

○ **정기 트리거**: 주기(매주·매월)로 무조건 재학습
  - 단순하고 예측 가능하지만, 변화 없어도 비용 발생, 변화가 주기 사이에 오면 늦는다

○ **이벤트 트리거**: 판단표가 경보를 확정할 때만 재학습
  - 필요할 때만 움직이지만, 판단 로직과 감시 인프라가 선행 투자

○ **혼합**: 정기 재학습으로 바닥을 깔고(점진 변화 흡수), 이벤트 경보 시 조기 실행
  - 실무의 기본값

○ 어느 방식이든 재학습 이력(언제·왜·어떤 데이터로)은 기록해야 한다

○ **같은 혼합 트리거라도 실행 경로는 도메인이 가른다**
  - 민간: 조건이 충족되면 파이프라인이 재학습·평가·배포까지 사람 손 없이 밟는 경우가 많다
  - 공공: 재학습은 검수 시점의 성능 근거를 무효화하는 변경 → 승인권자의 결재가 파이프라인의 한 단계로 박힌다(1-4의 결재선 분리와 같은 구조)
  - 공통: 어느 쪽이든 재학습 모델은 기존 승격 게이트를 다시 통과한다 — 자동화는 게이트를 건너뛰는 것이 아니라 사람 없이 통과시키는 것

#### 4-4. 루프의 완성

○ 재학습된 모델은 champion을 바로 교체하지 않는다
  - **challenger로 등록** → 9장 승격 게이트(절대 게이트 + champion 대비 개선) 통과
  - 통과하면 10장 배포 절차(테스트 층·카나리 또는 shadow)를 다시 밟는다
  - 게이트를 건너뛰면 9장에서 세운 감사 가능성이 끊긴다

○ 교재의 루프: 데이터(4~8장) → 훈련·등록(9장) → 배포(10장) → 감시(11장) → 재학습 → 다시 9장
  - MLOps가 "파이프라인"이 아니라 **"루프"**인 이유

#### 4-5. 역방향 — 이 장의 감시 설계를 민간 현장으로

○ **추천 서비스의 실시간 CTR 감시**
  - 골격은 10장 shadow 로그와 같다: 두 모델의 출력을 같은 요청 위에서 비교하고, 새 모델이 꺾이면 되돌린다
  - 바뀌는 것은 속도 — 레이블(클릭)이 즉시 오므로 표 11.5의 "성능 저하 확인" 칸을 몇 분 만에 채우고, 롤백을 자동으로 걸 수 있다
  - 민원 예측에서 자동 롤백을 걸 수 없는 이유가 여기서 드러난다: 레이블이 하루 늦으면 롤백도 하루 늦은 근거로 실행된다

○ **광고 클릭 예측의 계절 드리프트**
  - 3-2의 재발형(recurring)이 그대로 재현된다 — 연말·휴가철마다 클릭률 분포가 크게 이동
  - 대응 순서도 이 장 그대로: 원인 분류에서 "계절 재발" 판정(표 11.5 넷째 행) → 재학습 대신 계절 피처 추가나 시기별 임계
  - 여름철 대기질·연휴 뒤 민원 급증에 같은 판정을 내리는 것과 계산이 같다

○ **금융 사기 탐지의 컨셉 드리프트**
  - 사기 수법이 바뀌면 같은 거래 패턴(X)에서 사기 여부(Y)의 관계가 변한다 = P(Y|X)의 변화
  - 레이블 지연이 수일~수개월 → "레이블 없이 입력 분포를 먼저 감시"가 선택이 아니라 유일한 수단
  - PSI로 거래 패턴 분포를 감시하다 급변하면 전문가 검토 후 재학습 — 표 11.5의 경보 → 원인 분류 → 재학습 경로와 같다
  - 재학습 승인이 규제 준수 심사와 묶인다는 점은 오히려 공공의 결재선을 닮았다

○ **관통하는 하나**
  - 네 결정은 도메인과 무관하게 똑같이 내린다: ① 감시를 시스템·모델 두 층으로 나눈다 ② 레이블 도착 시점으로 1차 신호를 정한다 ③ 알림을 명령이 아니라 원인 분류의 착수 신호로 둔다 ④ 재학습 모델을 게이트에 다시 태운다
  - **근거만 도메인이 바꾼다** — 공공은 감사·형평성·법적 의무, 민간은 매출·비용·SLA
  - 근거가 다르면 임계의 값과 결재의 유무가 달라질 뿐, 판단의 순서는 그대로다
  - 층을 나란히 놓고 함께 읽어야 원인이 갈리는 것도 공통이다 — 매출 대시보드 옆 드리프트 대시보드, 민원 현황판 옆 분포 비교 기록

---

## [후반부] 실습 (50분)

---

### 실습 11.1: 기준 데이터와 현재 데이터의 분포 비교

#### 실습 목표
- 실측 스냅샷 두 쌍으로 KS·PSI·KL을 계산하고, 알림 판정과 권고 조치를 분리해 기록한다
- 2장 실습 2.2의 실측(KS 0.175, p 0.5786)을 이 장의 코드가 재현하는지 교차 검증한다
- 드리프트 알림 평가 보고서(정본 산출물)를 생성한다

#### 입력 데이터
- 서울 40개 측정소 PM10 실측 스냅샷 3종
- 기준: 2026-06-28 22:00 (2장 커밋 스냅샷 복사본)
- 비교 A: 2026-06-28 23:00 — 1시간 뒤 (2장 커밋 스냅샷 복사본)
- 비교 B: 2026-07-07 22:00 — 9일 뒤 (이 장에서 수집, 같은 시각 정렬)
- n=40 소표본, 변수 1개(PM10) — 관찰 대상은 분포 비교·판정 절차

#### 실행 환경
- Python 3.10+, numpy, pandas, scipy
- 서버·컨테이너 불필요 — 통계 계산만 수행

#### 실행 명령

```bash
cd practice/chapter11
python3 -m venv venv && source venv/bin/activate
pip install -r code/requirements.txt
python run_chapter11.py
```

#### 핵심 코드

```python
if ks_significant and psi >= PSI_ALERT:      # p<0.05 그리고 PSI>=0.25
    level = "경보"
elif ks_significant or psi >= PSI_WATCH:     # 둘 중 하나만
    level = "주의"
else:
    level = "정상"
```

_전체 코드는 practice/chapter11/code/11-1-drift-monitoring.py 참고_

#### 실행 결과 확인 사항

1. **2장 실측 재현**: 비교 A의 KS=0.175, p=0.5786이 `comparison_a_matches_ch2: true`로 출력되는가
2. **PSI=KL 항등식**: 두 비교 모두 `psi_equals_kl_sum: true`인가
3. **산출물 생성**: `ch11_drift_report.json`이 생성되고 재실행해도 바이트 동일한가
4. **마지막 줄**: `CH11_RUN_PASS`

**표 11.6** 분포 비교 실측(기준: 2026-06-28 22:00, n=40)

| 항목 | 비교 A: 1시간 뒤(6/28 23:00) | 비교 B: 9일 뒤(7/7 22:00) |
|---|---|---|
| 평균 | 20.025 → 20.775 | 20.025 → 17.75 |
| KS 통계량 / p-value | 0.175 / 0.5786 | 0.225 / 0.2657 |
| PSI(기준 분위수 5구간) | 0.1193 | 0.3324 |
| KL(기준→현재 / 현재→기준) | 0.0646 / 0.0548 | 0.1826 / 0.1497 |
| KS 유의(α=0.05) / PSI 대역 | False / 주의 | False / **경보** |
| 알림 등급 | **주의** | **주의** |

**표 11.7** 구간별 측정소 수(기준 분위수 5구간, PM10)

| 구간 | 기준(6/28 22:00) | 1시간 뒤(6/28 23:00) | 9일 뒤(7/7 22:00) |
|---|---|---|---|
| 17 미만 | 7 | 3 | **13** |
| [17, 19) | 5 | 6 | 7 |
| [19, 21) | 10 | 12 | 11 |
| [21, 22) | 5 | 3 | 5 |
| 22 이상 | 13 | 16 | **4** |

#### 관찰 과제

○ **관찰 1**: 1시간 간격에도 PSI가 "주의" 대역에 들어갔다
  - 비교 A는 2장에서 "값은 변하고 구조는 같다"로 판정된 쌍이다
  - KS는 유의하지 않은데 PSI는 0.1193으로 관행 임계 0.1을 넘었다
  - 원인: 5구간에 40건이면 구간당 기대 8건 — 측정소 두어 곳의 값이 구간 경계를 넘나들면 비율이 출렁이고 PSI가 쌓인다
  - 교훈: 관행 임계(0.1)를 소표본에 그대로 적용하면 평시에도 매시간 "주의"가 뜬다

○ **관찰 2**: 9일 뒤의 분포는 아래로 이동했고, 두 지표가 서로 다른 답을 냈다
  - PSI는 0.3324 → 경보 대역(≥0.25)
  - KS는 D=0.225, p=0.2657 → 유의하지 않음 (n=40에서 유의하려면 D가 대략 0.30을 넘어야 함)
  - "이동의 크기는 경보 수준인데, 이 표본 크기에서는 우연을 배제할 수 없다" → 조합 규칙이 **주의**로 판정
  - 표본이 400이었다면 같은 D로도 KS가 유의해져 경보가 성립했을 것

○ **관찰 3**: 알림 다음이 재학습이 아니라 원인 분류인 이유
  - 6월 말→7월 초의 계절·기상 요인이 유력하지만, 분포 수치만으로 원인을 특정할 수 없다
  - 원인 분류(계절/정책/시스템 변경/데이터 결함)가 먼저다(11.4 판단표)

#### 확장 과제 (선택)

1. PSI 임계를 0.2로 올려 비교 A의 판정이 바뀌는지 확인하라
2. n=40 대신 bootstrap으로 n=200을 만들어 KS가 유의해지는지 관찰하라
3. 알림 규칙을 AND에서 OR로 바꿨을 때 비교 B의 등급이 어떻게 변하는지 확인하라

---

### 과제 (구글 클래스룸 제출)

#### 제출물
다음 파일을 구글 클래스룸에 업로드한다.

- `practice/chapter11/data/output/ch11_drift_report.json`
- 실습 11.1을 실행해 생성된 드리프트 알림 평가 보고서 그대로 제출

#### 평가 기준
- 실습을 실행해 생성된 파일을 제출하면 완료로 인정한다(별도 배점 없음).

#### 마감
- 수업일 포함 7일 이내
